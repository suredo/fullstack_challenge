import { Container, Header, Cover } from "./style";

const Detail = () => {
  return (
    <Container>
      <Header>
        <Cover />
      </Header>
    </Container>
  );
};

export default Detail;
